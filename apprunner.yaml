version: 0.1
runtime: python311
build:
  commands:
    build:
      - pip3 install --upgrade pip
      - pip3 install -r requirements.txt
run:
  command: python3 -m flask --app app run
  network:
    port: 8080